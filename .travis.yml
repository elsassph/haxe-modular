language: haxe

haxe:
  - "3.4.4"
  - stable
  - development

matrix:
  allow_failures:
    - haxe: development

install:
  - node -v && npm install
  - haxelib install hxnodejs

script:
  - haxe build.hxml
  - node tool/test/test-suite.js
